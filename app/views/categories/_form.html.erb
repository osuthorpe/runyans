<%= simple_form_for @category do |f| %>

  <%= f.input :name %>

  <%= f.association :parent, collection: Category.where("id != #{@category.id}") %>

  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>
